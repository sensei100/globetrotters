<div class="container">
  <%= form_for @review do |f| %>
    <%= render 'form_fields', f: f %>
    <div class="actions">
      <%= f.submit "Submit", class: "btn btn-lg btn-primary" %>
    </div>
  <% end %>
</div>