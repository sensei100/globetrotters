<h1>Destinations</h1>

<%= button_to 'Add New Destination', new_destination_path, class: "btn btn-lg btn-primary add_dest" %>

  <% @destinations.each do |destination| %>
    <div>
      <p class="country"><%= destination.country %>
      <%= link_to destination.name, destination_path(destination), class: "dest_name" %></p>
      
      
      
  <% end %> 
    </div>
